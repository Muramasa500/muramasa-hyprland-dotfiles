profile {
    start = "20:00"
    end   = "06:00"
    temperature = 3500      -- color temperature in Kelvin (lower = warmer)
    gamma       = 0.8       -- perceived brightness (0-1)
    max-gamma   = 1.0
    identity    = false     -- if true, ignores temperature and uses only gamma
}
